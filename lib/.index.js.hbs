/**
 * {{pkg.description}}
 * @module {{pkg.name}}
 * @version {{pkg.version}}
 */

"use strict";

var execcli = require('./execcli'),
    pkg = require('../package.json');

var lib = execcli.bind(this);
lib.execcli = execcli;
lib.version = pkg.version;

module.exports = lib;
